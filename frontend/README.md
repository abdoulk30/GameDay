# GameDay — Frontend (Next.js + React)

Minimal Next.js frontend for GameDay.

Quick start:

```bash
cd frontend
npm install
npm run dev   # runs Next.js on port 3001
```

API:
- GET `/api/health` — proxies to backend `/api/health` (port 3000)

