# GameDay — Backend (Node + Express)

Minimal Express server for GameDay.

Quick start:

```bash
cd backend
npm install
npm run dev    # requires nodemon (dev) or use npm start
```

API:
- GET `/api/health` — basic health check

